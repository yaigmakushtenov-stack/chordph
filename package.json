{
  "name": "chordph",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@vercel/functions": "^3.1.1"
  }
}
